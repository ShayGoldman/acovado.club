# news-worker — required env vars
# Copy to ${ENV_FILES_ROOT}/news-worker.env on the VPS and fill in values.
# NODE_ENV and TRACE_EXPORTER_URLS are injected by docker-compose; do not set here.

DATABASE_URL=postgresql://acovado:CHANGE_ME@postgres:5432/production
RABBITMQ_URL=amqp://acovado:CHANGE_ME@rabbitmq:5672
NEWS_POLL_CRON=*/10 * * * *
