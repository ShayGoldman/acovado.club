# postgres — required env vars
# Copy to ${ENV_FILES_ROOT}/postgres.env on the VPS and fill in values.
# POSTGRES_DB is set by docker-compose (production); do not override here.

POSTGRES_USER=acovado
POSTGRES_PASSWORD=CHANGE_ME
