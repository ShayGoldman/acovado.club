{
  "name": "@apps/signal-processor",
  "private": true,
  "packageManager": "bun@1.1.36",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run src/index.ts",
    "dev": "bun --watch --inspect=localhost:16001/signal-processor src/index.ts",
    "lint": "bunx biome lint --write .",
    "format": "bunx biome format --write .",
    "check": "bunx biome check --write .",
    "clean": "rm -rf .turbo && rm -rf dist && rm -rf node_modules",
    "test": "bun test",
    "typecheck": "bun tsc --noEmit"
  },
  "devDependencies": {
    "@config/tsconfig": "workspace:*",
    "@config/typescript": "workspace:*",
    "@types/bun": "^1.1.13"
  },
  "dependencies": {
    "@modules/db": "workspace:*",
    "@modules/events": "workspace:*",
    "@modules/inference": "workspace:*",
    "@modules/logger": "workspace:*",
    "@modules/ticker-extractor": "workspace:*",
    "@modules/tracing": "workspace:*",
    "zod": "^4.0.0"
  }
}
